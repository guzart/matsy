import React from 'react';
import Button from 'matsy/button/Button';

export default function ButtonExample(props) {
  return (
    <Button
      accent={props.accent}
      colored={props.colored}
      contrast={props.contrast}
      disabled={props.disabled}
      raised={props.raised}
    >{props.children}</Button>
  );
}
